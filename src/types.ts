export type buttonProps = {
    textButton: string;
    classDop: string;
}

export type menuProps = {
    name: string;
}

export type menuFooterProps = {
    title: string;
    min: number;
    max: number;
}